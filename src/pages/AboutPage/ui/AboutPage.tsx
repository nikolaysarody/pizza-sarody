import About from '../../../entities/About/About';

const AboutPage = () => {
    return (
        <main className="app__content-left">
            <About />
        </main>
    );
};

export default AboutPage;
