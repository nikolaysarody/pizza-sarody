import { Addresses } from '../../../widgets/Addresses';

const AddressesPage = () => {
    return (
        <main className="app__content-left">
            <Addresses />
        </main>
    );
};

export default AddressesPage;
