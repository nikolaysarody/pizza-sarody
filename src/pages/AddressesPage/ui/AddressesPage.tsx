import { Addresses } from '../../../widgets/Addresses';

const AddressesPage = () => {
    return (
        <div className="app__content-left">
            <Addresses />
        </div>
    );
};

export default AddressesPage;
