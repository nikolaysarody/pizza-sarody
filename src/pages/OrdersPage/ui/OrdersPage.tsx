import { Orders } from '../../../entities/Orders';

const OrdersPage = () => {
    return (
        <main className="app__content-left">
            <Orders />
        </main>
    );
};

export default OrdersPage;
