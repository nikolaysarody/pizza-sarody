import { Checkout } from '../../../widgets/Checkout';

const CheckoutPage = () => {
    return (
        <main className="app__content-center">
            <Checkout />
        </main>
    );
};

export default CheckoutPage;
